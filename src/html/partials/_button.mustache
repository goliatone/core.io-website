            {{#isLink}}
            <a href="{{href}}" class="button{{#class}} {{.}}{{/class}}">{{txt}}</a>
            {{/isLink}}
            {{^isLink}}
            <button class="button{{#class}} {{.}}{{/class}}">
              <span>{{txt}}</span>
            </button>
            {{/isLink}}
